# MEXC Scalp Bot

Профессиональная торговая система для скальпинга на бирже MEXC с гибридной стратегией "Ёршики + Лесенка".

## 🚀 Особенности

- **Гибридная стратегия**: Комбинация двусторонних лимитных ордеров (Ёршики) и ступенчатых покупок (Лесенка)
- **Детектор режимов рынка**: Автоматическое определение режимов Quiet/Normal/Shock на основе ATR, z-score, OBI, TFI
- **Микроструктурные фильтры**: Защита от неблагоприятных рыночных условий
- **Риск-менеджмент**: Kill-switch, лимиты позиций, контроль просадки
- **Idempotency**: Безопасный рестарт с восстановлением состояния
- **Real-time мониторинг**: WebSocket потоки данных и консольная аналитика

## 📋 Требования

- Node.js 20+
- MEXC API ключи с правами на торговлю
- Минимум 100 USDC на балансе (настраивается)

## 🛠 Установка

1. **Клонировать проект**:
```bash
git clone <repository-url>
cd mexcscalp
```

2. **Установить зависимости**:
```bash
npm install
```

3. **Настроить переменные окружения**:
```bash
cp env.example .env
```

Отредактировать `.env` файл:
```env
# MEXC API Configuration
MEXC_API_KEY=ваш_api_ключ
MEXC_SECRET_KEY=ваш_secret_ключ
MEXC_BASE_URL=https://api.mexc.com
MEXC_WS_URL=wss://wbs.mexc.com/ws

# Database
DATABASE_PATH=./data/mexc_bot.db

# Logging
LOG_LEVEL=info
LOG_FILE=./logs/mexc_bot.log

# Bot Configuration
CONFIG_FILE=./config/defaults.ethusdc.json
SYMBOL=ETH/USDC
DEPOSIT_USD=100

# Risk Management
ENABLE_TRADING=true
DRY_RUN=false
```

4. **Компилировать TypeScript**:
```bash
npm run build
```

5. **Инициализировать базу данных**:
```bash
npm run db:init
```

## ⚙️ Конфигурация

Основные параметры настраиваются в файле `config/defaults.ethusdc.json`:

### Базовые настройки
```json
{
  "symbol": "ETH/USDC",
  "deposit_usd": 100,
  "fees_bps_round_trip": 0
}
```

### Риск-менеджмент
```json
{
  "risk": {
    "max_notional_pct": 0.20,        // Максимум 20% депозита в ордерах
    "max_inventory_pct": 0.03,       // Максимум 3% инвентаря
    "ladder_budget_pct": 0.12,       // 12% для лесенки
    "kill_switch_losses": 5,         // Stop после 5 убытков подряд
    "daily_dd_stop_pct": 0.03,       // Stop при просадке 3%
    "max_position_duration_sec": 600, // Максимум 10 минут в позиции
    "cancel_rate_per_min": 30        // Лимит отмен в минуту
  }
}
```

### Стратегия Ёршики
```json
{
  "hedgehog": {
    "levels": 4,                     // Количество уровней на сторону
    "offset_k_atr1m": 0.4,          // Оффсет = 0.4 * ATR_1m
    "step_k_atr1m": 0.5,            // Шаг = 0.5 * ATR_1m
    "tp_bps": {
      "quiet": 8,                   // TP 8 bps в тихом режиме
      "normal": 12,                 // TP 12 bps в нормальном режиме
      "shock": 20                   // TP 20 bps в шоковом режиме
    },
    "size_geometry_r": 1.1,         // Геометрическая прогрессия размеров
    "skew_alpha": 0.7               // Коэффициент для skew
  }
}
```

### Стратегия Лесенка
```json
{
  "ladder": {
    "steps": 6,                     // Количество ступеней
    "step_k_atr1m": 0.8,           // Шаг = 0.8 * ATR_1m
    "geom_r": 1.2,                 // Геометрическая прогрессия r=1.2
    "tp_bps": 12,                  // TP 12 bps
    "panic_tail_atr5m": 3.0,       // Паника при 3*ATR_5m
    "panic_close_frac": 0.4        // Закрывать 40% при панике
  }
}
```

## 🚀 Запуск

### Разработка
```bash
npm run dev
```

### Продакшн
```bash
npm start
```

### С Docker (опционально)
```bash
docker build -t mexc-scalp-bot .
docker run -d --env-file .env mexc-scalp-bot
```

## 📊 Мониторинг

Система выводит статистику каждые 30 секунд:

```
📊 Статистика системы {
  regime: 'normal',
  confidence: 0.75,
  activeLevels: 8,
  activeTPs: 3,
  inventory: 0.0234,
  inventoryNotional: -12.45,
  midPrice: 3250.67
}
```

### Логи
- Основные логи: `./logs/mexc_bot.log`
- Уровни: error, warn, info, debug
- Ротация логов по размеру

### База данных
SQLite база данных содержит:
- `events` - системные события
- `orders` - история ордеров
- `fills` - исполнения
- `positions` - позиции
- `trades` - завершенные сделки

## 🔧 Архитектура

```
src/
├── infra/          # Инфраструктура
│   ├── mexcRest.ts    # REST API клиент
│   ├── mexcWs.ts      # WebSocket клиент
│   └── rateLimiter.ts # Rate limiting
├── core/           # Ядро системы
│   ├── types.ts       # Типы данных
│   ├── math.ts        # Математические функции
│   ├── regime.ts      # Детектор режимов
│   └── orderbook.ts   # Обработка стакана
├── strategies/     # Торговые стратегии
│   ├── hedgehog.ts    # Стратегия Ёршики
│   ├── ladder.ts      # Стратегия Лесенка
│   └── hybrid.ts      # Гибридный оркестратор
├── risk/           # Риск-менеджмент
│   └── risk.ts        # Контроль рисков
├── storage/        # Хранение данных
│   ├── db.ts          # SQLite база
│   └── dao.ts         # Data Access Object
└── metrics/        # Метрики и аналитика
    ├── metrics.ts     # Расчет метрик
    └── dashboard.ts   # Консольный UI
```

## ⚡ Принцип работы

1. **Инициализация**: Загрузка конфигурации, подключение к API, инициализация базы
2. **Warm-up**: Накопление данных для индикаторов (ATR, VWAP, EMA)
3. **Детекция режима**: Анализ рыночных условий каждую секунду
4. **Котирование**: Выставление лимитных ордеров согласно режиму
5. **Исполнения**: Создание TP ордеров, восстановление уровней
6. **Риск-контроль**: Мониторинг лимитов, kill-switch при необходимости

## 📈 Стратегии

### Ёршики (Hedgehog)
- Двусторонние лимитные ордера вокруг mid-price
- 4 уровня на каждую сторону по умолчанию
- Автоматический skew при накоплении инвентаря
- Take-profit на каждое исполнение

### Лесенка (Ladder)
- Ступенчатые покупки ниже VWAP
- Активируется при z-score > 1.0
- Геометрическая прогрессия размеров
- Аварийный выход при экстремальных движениях

### Гибрид
- Базово работают Ёршики
- Лесенка включается при благоприятных условиях
- Адаптация к режимам рынка
- Единый риск-контроль

## 🛡️ Безопасность

### Риск-менеджмент
- **Position limits**: Максимальный инвентарь 3% депозита
- **Notional limits**: Максимум 20% депозита в ордерах
- **Kill-switch**: Автостоп при серии убытков
- **Daily drawdown**: Остановка при превышении дневной просадки
- **Duration limits**: Принудительное закрытие старых позиций

### Технические меры
- **Idempotency**: Уникальные clientOrderId для всех ордеров
- **Graceful shutdown**: Корректная отмена ордеров при остановке
- **Rate limiting**: Соблюдение лимитов API
- **Error handling**: Обработка всех типов ошибок API
- **Data validation**: Проверка всех параметров ордеров

## 🐛 Отладка

### Включить debug логи
```bash
LOG_LEVEL=debug npm run dev
```

### Проверить подключение к API
```bash
node -e "
const { MexcRestClient } = require('./dist/infra/mexcRest');
const client = new MexcRestClient(process.env.MEXC_API_KEY, process.env.MEXC_SECRET_KEY);
client.ping().then(console.log);
"
```

### Просмотр базы данных
```bash
sqlite3 ./data/mexc_bot.db
.tables
SELECT * FROM orders LIMIT 10;
```

## ⚠️ Важные замечания

1. **Реальная торговля**: Система работает с реальными деньгами. Тестируйте на малых суммах!

2. **API ключи**: Храните ключи в безопасности, используйте только необходимые права

3. **Мониторинг**: Следите за логами и метриками, особенно в первые дни

4. **Параметры**: Не меняйте параметры во время активной торговли

5. **Интернет**: Стабильное соединение критично для работы

6. **Резервное копирование**: Регулярно сохраняйте базу данных

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи в `./logs/mexc_bot.log`
2. Убедитесь в корректности API ключей
3. Проверьте баланс аккаунта
4. Проверьте права API ключей на торговлю

## 📄 Лицензия

MIT License - используйте на свой страх и риск.

---

**⚠️ ПРЕДУПРЕЖДЕНИЕ**: Торговля криптовалютами связана с высоким риском потери средств. Автоматические торговые системы могут работать не так, как ожидается. Используйте только те средства, потерю которых можете себе позволить. Авторы не несут ответственности за ваши торговые результаты. 